% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pssm-utils.R
\name{bind_pssms}
\alias{bind_pssms}
\title{Bind two PSSMs with an optional uniform-probability spacer}
\usage{
bind_pssms(pssm1, pssm2, gap = 0, bits_thresh = 0.1)
}
\arguments{
\item{pssm1, pssm2}{PSSMs to bind. Each should be a data frame or matrix with columns named \code{A}, \code{C}, \code{G}, \code{T}. Additional columns (such as \code{pos}) are ignored.}

\item{gap}{The number of uniform-probability spacer positions to insert between \code{pssm1} and \code{pssm2}. Must be a non-negative integer. Default is 0 (no spacer).}

\item{bits_thresh}{Either \code{NULL} (no trimming) or a numeric threshold passed to \code{\link{trim_pssm}} to trim low information-content positions from the beginning and end of each PSSM before binding. Default is 0.1.}
}
\value{
A data frame with columns \code{A}, \code{C}, \code{G}, \code{T} and a 0-based \code{pos} column, with rows ordered as (trimmed) \code{pssm1}, spacer, (trimmed) \code{pssm2}.
}
\description{
Concatenates two Position-Specific Scoring Matrices (PSSMs) along positions, optionally inserting a spacer of uniform-probability positions between them and optionally trimming low information-content edges from each PSSM beforehand. The spacer positions have 0 bits of information and therefore do not contribute to PWM scores; they simply enforce a fixed distance between the two motifs.
}
\examples{
pssm1 <- data.frame(
    A = c(0.9, 0.1, 0.1),
    C = c(0.05, 0.1, 0.1),
    G = c(0.025, 0.7, 0.1),
    T = c(0.025, 0.1, 0.7)
)
pssm2 <- pssm1

# bind directly
bind_pssms(pssm1, pssm2, bits_thresh = NULL)

# bind with a 5-bp neutral spacer between the two motifs
bind_pssms(pssm1, pssm2, gap = 5, bits_thresh = NULL)

}
\seealso{
\code{\link{trim_pssm}}, \code{\link{bits_per_pos}}
}
